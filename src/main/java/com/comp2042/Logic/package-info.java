/**
 * Contains the core business logic and data models for the Tetris game.
 * This includes classes for managing the board state (`Board`, `SimpleBoard`),
 * calculating scores (`Score`), handling game levels (`LevelManager`),
 * detecting collisions (`MatrixOperations`), and managing specific game mechanics like bombs.
 *
 * @author Abdullah Usmani
 */
package com.comp2042.Logic;